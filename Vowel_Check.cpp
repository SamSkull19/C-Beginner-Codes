#include<bits/stdc++.h>
using namespace std;

int main()
{
    char ch;
    cout << "Enter The Letter : ";
    cin >> ch ;

    if(ch == 'a' || ch == 'A'){
        cout << ch << " is a Vowel" << endl;
    }
    else if(ch == 'e' || ch == 'E'){
        cout << ch << " is a Vowel" << endl;
    }
    else if(ch == 'i' || ch == 'I'){
        cout << ch << " is a Vowel" << endl;
    }
    else if(ch == 'o' || ch == 'O'){
        cout << ch << " is a Vowel" << endl;
    }
    else if(ch == 'u' || ch == 'U'){
        cout << ch << " is a Vowel" << endl;
    }
    else{
        cout << ch << " is a Consonent" << endl;
    }
    return 0;
}